# Python dependencies for the model training script (train_price_forecast_model.py).
#
# The data collection and lookup-table scripts rely only on the standard
# library; only the XGBoost training step needs third-party packages. Install
# them into a virtual environment before training:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/python script/train_price_forecast_model.py
#
# Versions are lower-bounded, not pinned: the script uses stable, long-standing
# APIs. Note root_mean_squared_error requires scikit-learn >= 1.4.
xgboost>=2.0
scikit-learn>=1.4
pandas>=2.0
joblib>=1.3
